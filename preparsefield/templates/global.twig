{% import '_includes/forms' as forms %}

{{ forms.lightswitchField({
    label: "Disable all Preparse field columns on all element indices"|t,
    instructions: "While Preparse allows for per-element settings on whether or not a field should
                   be included in element tables, this can cause control panel performance issues on
                   sites with a large number of elements and fields."|t,
    name: 'disableElementTables',
    on:   settings.disableElementTables,
    first: true
}) }}
